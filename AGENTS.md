# AGENTS.md

This repo IS the master dev harness. Entry point: read `START.md` and follow it.

If you were sent here from a project, go back to that project after reading —
product work never happens inside this repo (only harness edits, which require
owner approval via the retro process in `core/SELF-IMPROVE.md`).
