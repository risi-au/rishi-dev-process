# retros/

Session retros land here as `YYYY-MM-DD-<project>-<slug>.md` (template:
`templates/retro.md`). A retro is data — it changes nothing until the owner
approves its proposals (`core/SELF-IMPROVE.md`). Processed retros move to
`archive/` during compaction sessions.
